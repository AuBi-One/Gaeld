# Static analysis for the plugin (the core phpstan.neon covers app/ only):
#   ./vendor/bin/phpstan analyse -c plugins/expense-claims/phpstan.neon
includes:
    - ../../phpstan.neon
parameters:
    paths:
        - src/
    bootstrapFiles:
        - phpstan-bootstrap.php
